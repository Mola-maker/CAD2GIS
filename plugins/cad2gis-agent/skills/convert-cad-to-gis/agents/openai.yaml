interface:
  display_name: "Convert CAD to GIS"
  short_description: "Evidence-first DWG conversion and registration"
  default_prompt: "Call get_capabilities first, require prompt contract v2, inspect this exact CAD source, submit only typed source-observed IDs, and report geometry, topology, length, and coordinate accuracy separately."
