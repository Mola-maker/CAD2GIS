"""Compatibility implementations for the historical experiment converter.

The maintained record reader and v3 pipeline do not load these rules unless a
caller explicitly invokes a legacy conversion entry point.
"""
